package learning

import "mentorforge-ai/backend-go/internal/domain"

func goCollectionsLesson() domain.Lesson {
	return domain.Lesson{
		ID:           7,
		TopicID:      1,
		Slug:         "go-lesson-03",
		Title:        "Условия, циклы и коллекции Go",
		Level:        "beginner",
		Goal:         "Научиться выбирать действие по условию, повторять операции в цикле, работать со slice и map, безопасно искать элементы и объяснять эти конструкции на техническом собеседовании.",
		Introduction: "В первых двух уроках вы познакомились с программой Go, переменными, типами, структурами, функциями и ошибками. Теперь программа научится принимать простые решения, повторять действия и работать не с одним значением, а с коллекцией учебных данных.",
		TheorySections: []domain.TheorySection{
			{
				Title:   "Зачем нужны условия",
				Content: "Программа часто должна выбирать действие. Например, учебную карточку с допустимым уровнем можно показать, а карточку с некорректным уровнем — отклонить. Выражение условия всегда даёт значение типа bool: true или false. Для сравнения используют ==, !=, >, <, >= и <=. Несколько проверок объединяют операторами && (и), || (или), а ! меняет true на false и наоборот. Начинайте с одного понятного условия, например level >= 1, и усложняйте проверку только при необходимости.",
			},
			{
				Title:   "if, else if и else",
				Content: "if выполняет свой блок, только когда условие равно true. else выполняется, если предыдущие условия оказались false. else if добавляет ещё один вариант между ними. В Go фигурные скобки обязательны, круглые скобки вокруг условия не нужны, а само условие должно иметь тип bool. Для уровня меньше 1 программа сообщает об ошибке, для уровня от 1 до 3 выбирает начальную карточку, а для уровня выше 3 — карточку повышенной сложности. Проверки идут сверху вниз, и выполняется первый подходящий блок.",
			},
			{
				Title:   "Цикл for",
				Content: "В Go для всех циклов используется ключевое слово for. Основная форма со счётчиком состоит из начального значения, условия продолжения, изменения счётчика и тела цикла: for i := 1; i <= 5; i++ { ... }. Перед первым шагом создаётся i, перед каждым шагом проверяется условие, после тела выполняется i++. Форма for condition { ... } похожа на while в других языках. Форма for { ... } создаёт бесконечный цикл, из которого обычно выходят через break. break полностью завершает ближайший цикл, а continue пропускает остаток текущей итерации и переходит к следующей. Метки и вложенные циклы в этом уроке не нужны.",
			},
			{
				Title:   "Массив и slice",
				Content: "Массив хранит фиксированное количество элементов, и его длина является частью типа: [3]string и [4]string — разные типы. Slice — удобное представление последовательности элементов одного типа; в прикладном Go-коде он используется чаще массива. Slice можно создать литералом, прочитать или изменить элемент по индексу, узнать длину через len и добавить элемент через append. В строке cards = append(cards, newCard) функция append возвращает обновлённый slice, поэтому результат нужно присвоить обратно. append может вернуть другое значение slice. cap показывает, сколько элементов может поместиться в доступной области до следующего расширения, но внутреннее устройство памяти и рост capacity пока не нужны.",
			},
			{
				Title:   "Обход slice через range",
				Content: "Запись for index, card := range cards последовательно обходит slice. Первое значение — индекс, второе — копия элемента. Если индекс не нужен, пишут for _, card := range cards. Если нужно работать только с индексом, пишут for index := range cards. Символ _ означает, что полученное значение намеренно не используется. Значение в переменной range является копией элемента. Для изменения самого элемента slice часто используют индекс: cards[index].Completed = true.",
			},
			{
				Title:   "Что такое map",
				Content: "map хранит пары ключ-значение. Ключ используют для быстрого поиска, все ключи имеют один тип, а все значения — другой общий тип. Например, map[string]StudyCard связывает строковое название с учебной карточкой. Map создают литералом или через make, после чего значения можно добавлять и изменять по ключу. delete(cardsByTitle, title) удаляет пару. Map подходит для индекса карточек по названию, но не заменяет упорядоченный список.",
			},
			{
				Title:   "Безопасный поиск в map",
				Content: "Конструкция card, ok := cardsByTitle[title] возвращает два результата. card содержит найденное значение или нулевое значение типа StudyCard. ok равно true, если ключ существует, и false, если ключ отсутствует. Проверять только card недостаточно: нулевое значение иногда является допустимым значением map. Поэтому безопасный код отдельно проверяет ok через if и возвращает понятную ошибку или сообщение при отсутствии ключа.",
			},
			{
				Title:   "Порядок обхода map",
				Content: "Порядок перебора map через range не гарантируется. Нельзя рассчитывать, что элементы будут идти в порядке добавления или одинаково при каждом запуске. Если нужен стабильный порядок, ключи обычно собирают в отдельный slice, сортируют и затем читают значения map по отсортированным ключам. Саму сортировку разберём позднее.",
			},
			{
				Title:   "nil и пустые коллекции",
				Content: "У nil slice длина равна нулю: его можно читать через len, безопасно обходить и передавать в append. Обращаться по индексу к пустому slice нельзя. Чтение отсутствующего ключа map возвращает нулевое значение, а дополнительный результат ok показывает отсутствие ключа. Запись в nil map приводит к panic во время выполнения. Перед записью map нужно создать литералом или через make: cards := make(map[string]StudyCard). Углубляться во внутреннее устройство nil сейчас не требуется.",
			},
			{
				Title:   "Где это используется в backend",
				Content: "Условия if проверяют входные данные HTTP-запроса и выбирают подходящий ответ. for и range обходят списки учебных пользователей, заказов или карточек. Slice часто хранит результаты запроса к хранилищу данных. Map используют для быстрого поиска, конфигурации, кэша и группировки. Проверка value, ok помогает безопасно работать с необязательными значениями и отличать отсутствующий ключ от допустимого нулевого значения.",
			},
		},
		Terms: []domain.Term{
			{Name: "bool", Explanation: "Логический тип с двумя значениями: true и false. Он используется в условиях и результатах сравнений."},
			{Name: "Условие", Explanation: "Выражение типа bool, по результату которого программа решает, выполнять ли блок кода."},
			{Name: "Ветвление", Explanation: "Выбор одной из нескольких веток выполнения с помощью if, else if и else."},
			{Name: "Итерация", Explanation: "Один отдельный шаг выполнения цикла."},
			{Name: "Цикл", Explanation: "Конструкция, которая повторяет блок кода, пока выполняется условие или пока цикл не будет остановлен."},
			{Name: "Индекс", Explanation: "Позиция элемента в последовательности. Индексы slice начинаются с нуля."},
			{Name: "Коллекция", Explanation: "Значение, которое объединяет несколько элементов. В этом уроке коллекциями служат slice и map."},
			{Name: "Массив", Explanation: "Последовательность фиксированной длины. Длина массива является частью его типа."},
			{Name: "slice", Explanation: "Удобная последовательность элементов одного типа, длина которой может меняться с помощью append."},
			{Name: "len", Explanation: "Встроенная функция, которая возвращает текущее количество элементов строки, массива, slice или map."},
			{Name: "cap", Explanation: "Встроенная функция, показывающая доступную вместимость slice. На начальном этапе достаточно знать, что cap может быть больше len."},
			{Name: "append", Explanation: "Встроенная функция добавления элементов в slice. Она возвращает обновлённый slice."},
			{Name: "range", Explanation: "Форма обхода элементов коллекции в цикле for. Для slice она даёт индекс и копию значения."},
			{Name: "map", Explanation: "Коллекция пар ключ-значение для поиска значения по ключу."},
			{Name: "Ключ", Explanation: "Значение, по которому выполняется поиск в map. Каждый ключ в одной map уникален."},
			{Name: "Значение", Explanation: "Данные, сохранённые в map под определённым ключом."},
			{Name: "Нулевое значение", Explanation: "Начальное значение типа без явного присваивания: например, 0 для int, false для bool и пустая строка для string."},
			{Name: "nil", Explanation: "Отсутствие созданного значения для некоторых типов. nil slice допускает len, range и append, но в nil map нельзя записывать элементы."},
		},
		CodeExamples: []domain.CodeExample{
			{
				Title:    "Условие для уровня карточки",
				Language: "go",
				Code: `level := 2

if level >= 1 {
	fmt.Println("Карточка доступна")
} else {
	fmt.Println("Уровень указан неверно")
}`,
				Explanation:    "Сравнение level >= 1 возвращает bool. При true выполняется первый блок, при false — блок else. Круглые скобки вокруг условия не нужны.",
				ExpectedOutput: "Карточка доступна",
			},
			{
				Title:    "Три диапазона уровня",
				Language: "go",
				Code: `level := 3

if level < 1 {
	fmt.Println("Некорректный уровень")
} else if level <= 3 {
	fmt.Println("Начальная карточка")
} else {
	fmt.Println("Карточка повышенной сложности")
}`,
				Explanation:    "Сначала проверяется level < 1. Оно ложно, поэтому программа переходит к level <= 3. Второе условие истинно, его блок выполняется, а ветка else уже пропускается.",
				ExpectedOutput: "Начальная карточка",
			},
			{
				Title:    "Цикл for со счётчиком",
				Language: "go",
				Code: `for number := 1; number <= 5; number++ {
	fmt.Println(number)
}`,
				Explanation:    "number создаётся со значением 1. Перед каждой итерацией проверяется number <= 5, после вывода выполняется number++. Когда number становится равен 6, цикл завершается.",
				ExpectedOutput: "1\n2\n3\n4\n5",
			},
			{
				Title:    "Другие формы for",
				Language: "go",
				Code: `attempt := 1
for attempt <= 3 {
	attempt++
}

for {
	break
}`,
				Explanation: "Первый цикл содержит только условие и похож на while из других языков. Второй формально бесконечный, но break сразу завершает его. continue вместо выхода пропустил бы остаток текущей итерации.",
			},
			{
				Title:    "Создание и изменение slice",
				Language: "go",
				Code: `cards := []string{"Основы Go", "Функции"}
firstCard := cards[0]
cards[1] = "Ошибки"

newCard := "Коллекции"
cards = append(cards, newCard)

fmt.Println(firstCard)
fmt.Println(len(cards), cards)`,
				Explanation:    "Литерал создаёт slice строк. Элемент читается и изменяется по индексу. append возвращает обновлённый slice, поэтому результат снова присваивается cards. Вместимость можно посмотреть через cap(cards), но её точное значение может отличаться, поэтому полагаться на него в логике программы не нужно.",
				ExpectedOutput: "Основы Go\n3 [Основы Go Ошибки Коллекции]",
			},
			{
				Title:    "Обход slice через range",
				Language: "go",
				Code: `type Card struct {
	Title     string
	Completed bool
}

cards := []Card{{Title: "Условия"}, {Title: "Циклы"}}

for index, card := range cards {
	fmt.Println(index, card.Title)
}

for _, card := range cards {
	fmt.Println(card.Title)
}

for index := range cards {
	cards[index].Completed = true
}`,
				Explanation:    "Первая форма получает индекс и копию элемента, вторая игнорирует индекс, третья использует только индекс. Изменение cards[index] меняет сам элемент slice; изменение переменной card изменило бы только копию.",
				ExpectedOutput: "0 Условия\n1 Циклы\nУсловия\nЦиклы",
			},
			{
				Title:    "Map и безопасный поиск",
				Language: "go",
				Code: `type StudyCard struct {
	Title string
	Level int
}

cardsByTitle := map[string]StudyCard{
	"Условия": {Title: "Условия", Level: 2},
}

title := "Условия"
card, ok := cardsByTitle[title]
if !ok {
	fmt.Println("Карточка не найдена")
} else {
	fmt.Println(card.Title, card.Level)
}

delete(cardsByTitle, title)`,
				Explanation:    "Строка служит ключом, StudyCard — значением. Дополнительная переменная ok надёжно отличает найденную карточку от отсутствующего ключа. delete удаляет пару после поиска.",
				ExpectedOutput: "Условия 2",
			},
			{
				Title:    "nil slice и созданная map",
				Language: "go",
				Code: `var titles []string
titles = append(titles, "Коллекции")

cards := make(map[string]StudyCard)
cards["Коллекции"] = StudyCard{Title: "Коллекции", Level: 2}

fmt.Println(len(titles), len(cards))`,
				Explanation:    "append допустим для nil slice. Map перед записью создаётся через make; попытка записать элемент в nil map вызвала бы panic.",
				ExpectedOutput: "1 1",
			},
		},
		RealUsage: []string{
			"if проверяет обязательные поля и допустимые значения входного HTTP-запроса.",
			"for и range обходят списки учебных пользователей, заказов или карточек.",
			"slice хранит последовательность результатов, полученных из хранилища данных.",
			"map помогает быстро находить значение, хранить конфигурацию, строить кэш или группировать элементы.",
			"value, ok безопасно обрабатывает необязательные значения и отсутствующие ключи.",
		},
		KeyPoints: []string{
			"Условие if должно иметь тип bool.",
			"В Go для всех форм циклов используется for.",
			"Slice хранит последовательность элементов одного типа.",
			"append возвращает обновлённый slice, поэтому результат обычно присваивают обратно.",
			"range позволяет обходить slice и map.",
			"Для slice range возвращает индекс и копию значения.",
			"Map хранит пары ключ-значение.",
			"Наличие ключа map проверяют через value, ok.",
			"Порядок обхода map не гарантируется.",
			"Перед записью в map её нужно создать через make или литерал.",
			"Индекс за пределами slice вызывает panic.",
		},
		CommonMistakes: []string{
			"Использовать = вместо == в сравнении. = выполняет присваивание, а == сравнивает значения. Правильно: if level == 2 { ... }.",
			"Ожидать, что условием может быть число или строка. В Go условие обязано иметь тип bool. Правильно: if len(cards) > 0 { ... }.",
			"Забыть присвоить результат append обратно. Правильно: cards = append(cards, newCard).",
			"Обращаться к несуществующему индексу slice. Перед cards[index] проверьте: if index >= 0 && index < len(cards) { ... }.",
			"Путать индекс и значение в range. В for index, card := range cards переменная index содержит позицию, а card — копию элемента.",
			"Пытаться изменить элемент через копию range-переменной. Для изменения используйте индекс: cards[index].Completed = true.",
			"Считать, что map сохраняет порядок добавления. Порядок range по map не гарантируется; для стабильного порядка нужны отдельный slice ключей и сортировка.",
			"Не проверять ok при поиске ключа. Правильно: card, ok := cardsByTitle[title]; if !ok { ... }.",
			"Пытаться записать значение в nil map. Сначала создайте её: cardsByTitle := make(map[string]StudyCard).",
			"Использовать map там, где нужен упорядоченный список. Для последовательного порядка храните элементы в slice, а map добавляйте только как индекс для поиска.",
		},
		Summary:        "Условия выбирают действие, for повторяет его, slice хранит упорядоченную последовательность, а map связывает ключи со значениями. Безопасный код проверяет границы slice, сохраняет результат append, создаёт map до записи и использует value, ok при поиске.",
		QuestionIDs:    []int{31, 32, 33, 34, 35, 36, 37, 38, 39},
		PracticeTaskID: 7,
	}
}

func goCollectionsPracticeTask() domain.PracticeTask {
	return domain.PracticeTask{
		ID:          7,
		LessonID:    7,
		Title:       "Каталог учебных карточек",
		Description: "Создайте небольшую консольную Go-программу для хранения, фильтрации и безопасного поиска нейтральных учебных карточек. Конкретные карточки, реализацию функций и формат строк вывода определите самостоятельно.",
		Requirements: []string{
			"Использовать структуру StudyCard с обязательными полями Title string, Topic string, Level int и Completed bool.",
			"Создать slice минимум из пяти учебных карточек с разными темами и уровнями.",
			"Вывести все карточки через for range.",
			"Создать функцию фильтрации карточек по минимальному уровню.",
			"Функция фильтрации должна вернуть новый slice.",
			"Обработать минимальный уровень меньше 1 через error.",
			"Создать map, где ключ — название карточки, а значение — StudyCard.",
			"Создать функцию поиска карточки по названию.",
			"Поиск должен возвращать StudyCard и error и проверять переменную ok.",
			"Проверить успешный поиск существующей карточки.",
			"Проверить поиск отсутствующей карточки.",
			"Обработать пустое название для поиска через error.",
			"Изменить Completed хотя бы у одной карточки.",
			"Вывести понятный результат каждого этапа, включая успешные и ошибочные сценарии.",
			"Отформатировать код через gofmt и запустить программу через go run.",
		},
		ExpectedResult: "Все карточки:\n...\n\nКарточки с уровнем не ниже 2:\n...\n\nНайдена карточка:\n...\n\nОшибка поиска:\n...",
		Hints: []string{
			"Подсказка 1: slice структур можно создать литералом: cards := []StudyCard{...}. Самостоятельно заполните минимум пять карточек.",
			"Подсказка 2: для обхода используйте for index, card := range cards { ... }; ненужный индекс замените на _.",
			"Подсказка 3: подходящие карточки добавляйте в новый slice через result = append(result, card).",
			"Подсказка 4: индекс по названию создайте через make(map[string]StudyCard), затем заполните его в цикле.",
			"Подсказка 5: наличие карточки проверяется так: card, ok := cards[title]. Если ok равно false, верните ошибку.",
			"Подсказка 6: простую ошибку можно создать через errors.New(\"понятное сообщение\"). Полную реализацию функций напишите самостоятельно.",
		},
		StarterCode: `type StudyCard struct {
	Title     string
	Topic     string
	Level     int
	Completed bool
}

func filterByMinLevel(cards []StudyCard, minLevel int) ([]StudyCard, error)

func findCardByTitle(cards map[string]StudyCard, title string) (StudyCard, error)`,
		Language: "go",
	}
}
